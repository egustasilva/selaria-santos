/**
 * Chaves do Supabase. PREENCHA com os valores do seu projeto:
 *   Project Settings → API → "Project URL" e "anon public" key.
 *
 * A anon key é PÚBLICA por design (vai no navegador). A segurança vem das
 * regras RLS no banco (db/schema.sql), não do sigilo da chave.
 */
export const SUPABASE_URL = 'COLE_AQUI_A_PROJECT_URL';
export const SUPABASE_ANON_KEY = 'COLE_AQUI_A_ANON_KEY';
